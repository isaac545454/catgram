export interface deletePhotoReq {
  id: string;
}

export interface UpdatePhoto {
  title: string;
  id: string;
}

export interface PropsEdit {
  title: string;
  id: string;
  profileImage: string;
}
